# DATABASE_URL: Set your own PostgreSQL connection string in a private .env file
# or the hosting environment. Never commit real credentials.
DATABASE_URL="postgresql://USERNAME:PASSWORD@HOST/DATABASE?sslmode=require"
